import type { ExtractedShapeSource } from '../contracts/ProcessedShape'

export async function extractRasterShape(_imageSource: string): Promise<ExtractedShapeSource | undefined> {
  return undefined
}
