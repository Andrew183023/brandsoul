This directory is a compatibility facade.

The canonical PersonaLab domains now live under `src/domain/*`, `src/features/persona-lab/*`, `src/runtime/*`, and `src/export/*`. Keep this path only while old imports are migrated. Do not add new modules here.
